// Aqui podemos adicionar interatividade simples
document.querySelectorAll('.btn').forEach(button => {
    button.addEventListener('click', () => {
        alert('🌿 Obrigado por se interessar pelo agro sustentável!');
    });
});
